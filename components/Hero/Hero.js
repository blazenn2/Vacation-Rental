import HeroContent from "./HeroContent"

const Hero = () => {
    return (
        <div className="h-[90vh] bg-hero bg-cover grid grid-cols-2">
            <HeroContent />
        </div>
    )
}

export default Hero
